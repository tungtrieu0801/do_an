import 'package:flutter/material.dart';

class thanksTablet extends StatelessWidget {
  const thanksTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text("thanks tablet screen ")),
    );
  }
}
