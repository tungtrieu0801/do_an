import 'package:flutter/material.dart';

class thanksDesktop extends StatelessWidget {
  const thanksDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text("thanks desktop screen ")),
    );
  }
}
