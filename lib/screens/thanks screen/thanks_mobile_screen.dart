import 'package:flutter/material.dart';

class thanksMobile extends StatelessWidget {
  const thanksMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text("Thanks mobile screen ")),
    );
  }
}
